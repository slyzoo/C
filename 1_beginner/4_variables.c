#include <stdio.h>
int main(){

    // variable a thing to hold strings, integers, booleans, and floats

    // declaration
    int x;

    // initialization
    x=14;

    // both declaration and initalization
    int y = 140;

    // integer
    int age = 17;

    // float
    float gpa = 2.2;

    // character
    char grade = 'C';

    // string
    char name[] = "slyzoo"; // technically an array of characters

printf("hello %s \n", name);

printf("you are %d years old\n", age);

printf("your grade average is %c\n", grade);

printf("your gpa is %f\n", gpa);



    return 0;
}